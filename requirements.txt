# Compatibility entry point for pip users.
#
# pyproject.toml and uv.lock are the authoritative dependency contract. This
# file intentionally installs that project instead of maintaining a second,
# conflicting requirements list.
.
